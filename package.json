{
  "name": "niconicoviwer",
  "version": "1.0.0",
  "description": "niconico comment viewer",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron ./app",
    "build-macOS": "electron-packager ./app FirstApp --platform=darwin --arch=x64 --overwrite",
    "build-windows": "electron-packager ./app FirstApp --platform=win32 --arch=x64 --overwrite"
  },
  "author": "takuminish",
  "license": "ISC",
  "dependencies": {
    "ejs": "^2.6.1",
    "express": "^4.16.4"
  }
}
